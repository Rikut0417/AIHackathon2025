steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'functions'
      - 'deploy'
      - 'Hack-Syumi'
      - '--runtime=python311'
      - '--region=global'
      - '--source=.'
      - '--trigger-http'
      - '--entry-point=main_function'
